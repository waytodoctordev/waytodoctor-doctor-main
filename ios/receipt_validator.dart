import 'package:flutter/services.dart';  // For platform channels
import 'package:in_app_purchase_storekit/in_app_purchase_storekit.dart';


// String certificate = "StoreKitTestCertificate";
// String certificatew = "AppleIncRootCertificate";
//
// #if (dart:io.Platform.isIOS) && (dart:environment 'FLUTTER_TEST' == 'true')
// // This block will be included only for iOS in debug mode (with `FLUTTER_TEST=true`)
// final String certificatePath = 'path/to/your/test.cer'; // Replace with actual path
// #else
// // This block will be included for all other scenarios (non-iOS, release mode)
// final String certificatePath = 'path/to/your/production.cer'; // Replace with actual path
// #endif
